# Tactics Grid Example

A fictional 6x6 level-balancing demo.

It shows the Playable AI contract:

```text
grid state -> task pack -> mock provider -> candidate cards -> apply / ignore
```

This is a game/editor example, not a real-world military or operational decision tool.

Run it:

```bash
pnpm --filter @playable-ai/example-tactics-grid dev
```

