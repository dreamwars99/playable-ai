# Kanban Quest Example

A small productivity-board demo.

It shows that Playable AI is not only for games. A normal app can serialize board state, run a task pack, and show candidate cards before changing the host app state.

Run it:

```bash
pnpm --filter @playable-ai/example-kanban-quest dev
```

