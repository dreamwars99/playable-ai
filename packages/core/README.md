# playable-ai core

Framework-neutral primitives for turning app state into AI tasks and reviewable candidates.

```ts
import { createTask, createCandidate } from "playable-ai";
```

The core package has no React, DOM, Vite, or provider dependency. Host apps bring their own UI, backend, database, provider adapter, and operation mapper.

