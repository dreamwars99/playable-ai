# Packages

Reusable Playable AI packages live here.

Current packages:

- `core`: framework-neutral task, candidate, operation, task pack, prompt pack, queue, and provider adapter primitives
- `react`: optional React hooks and helpers for task/candidate UI state
- `server`: optional backend/provider execution helpers

The core package should stay free of React, DOM, Vite, and provider-specific dependencies.
